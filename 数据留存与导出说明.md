# 光阴对赌 · 数据留存与导出说明

> 本文说明：在本项目中要做什么、不做什么，以及行为数据应如何「只记事实、方便导出」，供后续在**项目外**做机器学习与聚类分析。  
> 报告页面的文案与视觉打磨、问卷内容编写，均不在本文范围内，由人工另行完成。

---

## 一、我们要解决什么问题

《光阴对赌》一局结束后，玩家会看到排行榜、人格分析、PDF 等「报告」类内容。团队下一步的重点，不是立刻把报告写得更花，而是：

1. **在游戏过程中留下完整、可核对的行为记录**（高维但诚实，不掺推断出来的「心理指标」）。
2. **在一局结束时把这些记录导出**，交给另一套工具做降维、聚类、和问卷对照等分析。
3. **正式游戏里不出现问卷**；问卷由人编写，只在分析阶段使用，用来辅助理解无监督聚类结果。
4. **现有的「命运素描」和「决策基因」两套分类先保留**，等外部 ML 有结论后，再决定是否调整大框架。

机器学习、降维、选簇数量、写聚类报告，**都不在本仓库里实现**。

---

## 二、工作边界（一张表看清分工）

| 事项 | 在本项目（光阴对赌网站/服务端） | 在项目外 |
|------|--------------------------------|----------|
| 记录玩家操作与局内状态变化 | ✅ | |
| 局末导出结构化数据（如 JSON） | ✅ | |
| 无监督聚类、降维、调参 | | ✅ |
| 问卷设计、发放、录入 | | ✅（不进正式游戏） |
| 主持/观察员笔记与评分表 | | ✅（可与导出数据按局号、玩家 ID 对齐） |
| 终局报告 UI、PDF 文案与版式 | 现有能力保留；深度打磨 | ✅（主要由人完成） |
| 用 ML 结果改写 16 型 / 命运素描框架 | 暂缓，等 ML 后再议 | 参与决策 |

---

## 三、当前项目里和「数据」相关的现状

便于对齐预期，下面只写和留存、导出相关的事实。

**已经有的：**

- 局末会算出一套 `analysisResult`（五维分数 + 命运素描类型 + 决策基因四字母），在 `GAME_OVER` 展示。
- 玩家身上有累计字段：长期/风险精力投入、总消耗精力、财富曲线 `wealthHistory`、用过的道具 ID 列表、主持人打的社交档 `socialRank`（A–E）、终局对两套描述的投票 `personaVote`。
- 每个项目有全局的 `investorRecords`、`earningRecords`（谁投了多少、谁拿了多少收益）。
- 转账有 `transactions` 数组（谁给谁、金额、备注、是否接受、时间戳）。
- 全局有文字 `logs`（投资完成、用卡、拍卖、评分等），但**不适合当唯一数据源**（难解析、会丢结构）。

**明显不足的：**

- 每轮结算详情只保存在 `lastSettlement`，**新的一轮会覆盖上一轮**，历史轮次无法从状态里完整还原。
- 拍卖「出价多少、接受还是拒绝」主要写在 `logs` 里，**没有统一的事件表**。
- 单次投资提交只更新当前轮投入，**没有「第 N 轮、第 M 个项目、投了多少」的时间序列快照**（部分可从结算与项目记录反推，但不完整、也不等于当时屏幕上的每一次改单）。
- 房间数据在内存里，**关掉服务或散房即消失**，目前没有标准导出接口。
- 设计文档里写过的一些指标（例如「跟投」「号召力」）**不能**当成已经能采集的数据——见下一节。

---

## 四、数据原则：只记能直接观察到的事

后续分析可以自己构造特征，但**本项目留存层**应遵守：

1. **一条记录对应一个可核对的事实**（谁、何时、对什么对象、做了什么、数值是多少）。
2. **不在留存阶段写「猜测型」标签**，例如：
   - 「跟投」：需要定义「先投的人带动后投的人」并做因果推断，不能从单次投资表里直接读出。
   - 「垄断拍卖」「社交影响力」等，若没有在系统里登记出价次数、溢价金额，就不能假装已有。
3. **可以保留局末规则算出来的 `analysisResult`**，但应标明来源是「内置规则引擎」，与「原始事件流」分开存放，避免和 ML 特征混为一谈。
4. **文字 logs 可作为附录**，不能替代结构化事件；导出时以结构化事件为主。

这样外部做 ML 时，特征工程有清晰起点；做效度检验时，也能分清「事实」和「解释」。

---

## 五、建议纳入留存与导出的内容（均可从玩法中直接对应）

下列是**按当前代码能力**能定义、或稍加扩展即可在服务端落库的内容。实现细节后续再定，这里只定「记什么」。

### 5.1 局元数据（一局一行）

- 房间 ID、开始/结束时间、社区名称。
- 玩家列表（ID、昵称、是否 AI）、人数。
- 时代顺序 `eraSequence`、各时代主题卡。
- 本局出现的项目池（ID、名称、类型、时代等静态信息）。

### 5.2 玩家静态与终局快照

- 选座结果 `draftOrder`（含偷天换日等导致的**交换记录**，应记成交换事件，而不只记最终座位）。
- 终局：财富、精力、排名、各长期项目状态。
- 主持人社交评分 `socialRank`（及评分发生时间，若可记）。
- 玩家终局投票 `personaVote`（若已投）。
- 可选：`analysisResult` 整包（标明 `computedBy: "rule_engine_v1"`）。

### 5.3 事件流（建议核心，一行一个事件）

推荐用统一字段，例如：`eventType`、`timestamp`、`playerId`、`payload`。

| 事件类型（示例） | 能直接记录的内容 |
|------------------|------------------|
| `investment_submitted` | 全局轮次、时代、本轮各项目投入精力（提交时的整包 `investment`） |
| `buff_used` | 卡牌 ID、目标玩家/目标项目（若有）、是否成功 |
| `transaction_created` / `transaction_resolved` | 发起方、接收方、金额、备注、接受/拒绝 |
| `coffee_purchased` | 花费财富、获得精力 |
| `auction_offered` | 主持发起的卡 ID、标价 |
| `auction_resolved` | 接受/拒绝、实付财富、卡是否入背包 |
| `lottery_settled` | 主持录入的金额（可正可负） |
| `social_rated` | 被评玩家、档位 A–E |
| `phase_changed` | 阶段名、全局轮次等 |
| `settlement_round` | **整轮** `SettlementProjectResult` 快照（每轮一份，不要只留最后一轮） |

说明：

- **投资**：即使不记前端每次拖动滑块，至少应记**每次提交**时的完整分配；若以后要记「改单次数」，需要前端额外上报，目前代码没有。
- **道具**：`usedCards` 只有 ID 列表，**丢失目标对象**；导出层应改为从 `useBuffCard` 成功时写事件（卡 ID + target）。
- **项目维度累计**：局末仍可导出每个项目的 `investorRecords` / `earningRecords` / 是否完成或爆雷，与事件流互相印证。

### 5.4 不建议写进「事实层」的内容（可在外部分析时再算）

- 跟投、带头投资、社交中心度等图论/时序推断指标。
- 从 logs 用正则「猜」出来的拍卖次数（除非先把拍卖改成结构化事件）。
- 把主持人一句话点评自动当成多维人格分数。

外部分析若需要这些，应基于事件流**自己写规则或模型**，并单独文档说明假设。

---

## 六、导出形态与用途

**目标**：一局结束（进入 `GAME_OVER` 且人格分析算完后）生成**一份可下载、可归档**的文件，例如：

- 单文件 JSON：`session_export_{roomId}_{isoTime}.json`  
  或拆成 `meta.json` + `events.jsonl` + `players.json`。

**建议结构（概念上三块）：**

1. `meta`：局信息、版本号（`exportSchemaVersion`）、游戏规则/权重版本（若 `AnalysisWeights` 会变，应记下 hash 或版本）。
2. `players`：玩家信息与终局快照。
3. `events`：按时间排序的事件数组；以及**每轮结算**数组 `settlements[]`（与 `lastSettlement` 同级但保留全部轮次）。

**用途：**

- 导入 Python/R 等环境做聚类、降维、可视化。
- 与**同局、同玩家 ID**的问卷行、主持观察表做 join（问卷不在游戏里填，用 Excel/数据库键对齐即可）。

本项目**不需要**实现聚类算法、不需要在导出里预置「第几类人格」字段（除非仍想附带规则引擎的 `analysisResult` 作对照）。

---

## 七、问卷与主持观察（场外，如何配合）

- **问卷**：由人编写，针对真实玩家或实验被试，在局前/局后填写；字段设计应服务于「解释聚类」或「检验聚类是否与自陈一致」，而不是替代行为数据。
- **主持观察**：游戏里已有社交 A–E；若观察员还有更多维度（谈判、是否主动结盟等），应记在**场外表**里，通过 `roomId` + `playerId` 与导出 JSON 关联。
- **对齐方式**：导出 JSON 里的玩家 ID 在一局内稳定；问卷、观察表使用同一 ID 或「局号 + 座位号」映射表，由分析同学在项目外维护。

无监督聚类本身不需要问卷；问卷的价值在于：**簇有没有意义、叫什么名字、要不要反馈到正式报告**，这是人的工作，也在 ML 项目里完成。

---

## 八、「命运素描」与「决策基因」怎么处理

两套分类都是**规则 + 权重**算出来的（`analysisLogic.ts`），和「从数据里长出来的簇」不是一回事。

**当前决定：**

- 游戏内展示、投票、PDF 等**继续保留**两套结果，不因为要做 ML 就先拆掉。
- 导出时把 `analysisResult` 作为**可选附录**，与 `events` 分开，避免分析时误把规则标签当成 ground truth。
- 等外部聚类稳定、和问卷/观察对过几轮之后，再开会决定：是保留 16 型作「对外叙事」，还是用新簇作叙事，或两套并存。

---

## 九、和「报告体验打磨」的关系

终局页（排行榜、雷达图、四轴条、PDF 等）已有基础。后续由人改文案、加叙事、做对比图，**可以消费**：

- 规则引擎的分数（现状）；
- 将来可选的「分析组提供的簇 ID / 簇描述」（通过手工配置或二次导入，本阶段不承诺自动对接）。

数据留存与导出**不依赖**报告 UI 是否精美；反过来，好的报告需要可靠的数据，所以先做导出底座是合理的顺序。

---

## 十、小结

| 结论 | 说明 |
|------|------|
| 方向清楚 | 本项目专注「诚实、结构化、可导出」的行为数据；ML 与问卷在体外闭环。 |
| 可行 | 玩法里投资、道具、转账、结算、主持评分等已能对应到事件；主要工作是**补全轮次历史、结构化拍卖与用卡目标、局末导出**，而不是重做游戏。 |
| 纪律重要 | 不写「跟投」这类推断字段进事实层，避免分析建立在空中楼阁上。 |
| 分类暂留 | 命运素描 + 决策基因继续服务玩家；与 ML 簇的关系以后再定。 |

---

*文档版本：2026-09 · 与仓库现状对齐；实现导出 API 与事件表时，请同步更新 `exportSchemaVersion`。*
